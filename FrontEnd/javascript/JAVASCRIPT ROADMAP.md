JAVASCRIPT ROADMAP...(MUST LEARN)

1.VARIABLES
    a.Var
    b.Let
    c.Const

2.DATA TYPES.
    a.string
    b.number
    c.boolean
    d.array
    e.object
    f.null
    g.undefined

3.OPERATORS
1.ARITHMATIC OPERATORS
    a.(+) Addition
    b.(-) Subtraction
    c.(*) Multiplication
    d.(/) Division
    e.(%) Modulus/Reminder
    f.(**) Exponentiation
    g.(++) Increment by One
    h.(--) Decrement by One

2.ASSIGNMENT OPERATORS
    a.(=) Assign
    b.(+=) Add and Assign
    c.(-=) Sub and Assign
    d.(*=) Mul and Assign
    e.(/=) Div and Assign
    f.(%=) Mod and Assign
    g.(**=) Exponentiation and Assign

3.COMPARISION OPERATORS
    a.(==) Equal value only
    b.(===) Equal value and type (Most Useful)
    c.(!=) Not Equal value only
    d.(!==) Not Equal value or type
    e.(>) Greater than
    f.(<) Less than
    g.(<=) Less than or Equal to
    h.(>=) Greater than or Equal to
    
    4.LOGICAL OPERATORS
    a.(&&) Logical AND true if both are true
    b.(||) Logical OR true if any one is true
    c.(!)  Logical NOT 
    
    5.TERNARY OPERATOR (Shorthand of if..else)

    6.UNARY OPERATOR (Perform Operations on a single line)

4.CONDITIONAL STATEMENTS
    a.If Statement
    b.If Else Statement
    c.Else If Ladder 
    d.Nested If Statement
    d.Switch Statement

5.LOOPS
    a.For
    b.while
    c.Do While
    d.For..of
    e.For..in
    f.For..Each

6.FUNCTIONS
To learn:
    a.Normal Functions
    b.Arrow Functions
    c.Function Parameters
    d.Return Statements

7.ARRAYS
To learn:
    a.Push()
    b.Pop()
    c.shift()
    d.unshift()
    e.includes()
    f.indexOf()
    g.length

8.ARRAY HIGHER ORDER FUNCTIONS (VERY IMPORTANT)
To learn:
    a.map()
    b.filter()
    c.reduct()
    d.forEach()

9.OBJECTS
To learn:
    a.Object Creations
    b.Access properties
    c.Update Values
    d.Spread Operators

10.ES6 FEATURES
Modern Javascript
To learn:
    a.spread operators
    b.Destructuring
    c.Template literals
    d.Arrow Functions
    e.Default parameters

11.DOM MANUPULATION
To learn:
    a.getElementById()
    b.getElementByClass()
    c.querySelector()
    d.addEventListner
    e.onclick
    f.changing HTML & CSS using JS

12.EVENTS
Eg:
    a.click
    b.submit
    c.mouseover
    d.keydown

13.ASYNCHRONOUS JAVASCRIPT
To Learn:
    a.setTimeout()
    b.setInterval()
    c.Promise
    d.async/await
    e.fetch API
    f.callback function

14.(MOST MOST IMPORTANT)
Problem Solving:
    a.Reverse a string
    b.Find even numbers
    c.Remove Duplicates
    d.Count vowels
    e.Sum of Array





    
